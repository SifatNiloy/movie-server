import { table } from 'console';
import React from 'react';
import UserTable from './UserTable';

const page = async() => {
    
    return (
        <div>
           <UserTable/>
        </div>
    );
};

export default page;